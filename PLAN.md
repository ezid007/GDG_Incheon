# 프로젝트 구조 정비 계획

확인일: 2026-09-13. 기존 계획은 `work/cleanup-2026-09-13/PLAN.before.md`에 보존한다.

## 목적과 요구사항

해커톤 단기 제작으로 섞인 화면 코드와 문서를 정돈한다. 배포는 중지하고, 기존 콘텐츠·사진 역할·게임 기록·QR 공유·모바일 동작을 유지하면서 코드와 파일 관리 전반을 정비한다. 완료 후 재배포는 사용자가 요청할 때 진행한다.

## 범위와 구현 단계

| 상태 | 작업 |
|---|---|
| 완료 | 배포 중지, 현재 구조와 기준 결과 보존 |
| 완료 | HTML/CSS/브라우저 동작 분리 및 읽기 쉬운 코드 정리 |
| 완료 | 현재 문서와 과거 기록 구분, 향후 작업 가이드·변경 요약 작성 |
| 완료 | 빌드·전체 검사·모바일/PC 핵심 흐름 검증 후 커밋·푸시 |

영향 파일: `.github/workflows/deploy-pages.yml`, `apps/golmok-detective/src/`, 빌더, 기존 템플릿, 관련 tests, 생성 public HTML, README, PLAN, 관련 docs, 로컬 logs/development.log 및 Git 제외 설정.

검증: 기존 76개 검사 기준 유지. 생성 페이지의 스크립트·DOM·사진 역할 보존 확인, 빌드 조립 회귀 검사, 소개→지도→도착→오답/정답→완료·복원 및 QR/복사, 320/390/1280px 표시. 공개 재배포는 실행하지 않는다.

## 파일 관리와 위험

- `.env`, 개인 자료, 로그, 원격 첨부는 공개하지 않는다. 사진 원본을 삭제하지 않는다.
- 현재 작업 안내는 루트 PLAN과 docs의 현행 문서, 이전 설명은 archive로 구분한다.
- 자동 생성 HTML은 소스에서 다시 만든다. 생성물을 직접 수정하지 않는다.
- 프레임워크·런타임 의존성 추가 없이 기존 Node 표준 모듈과 명령을 유지한다.
- 파일 분리로 빌드 경로가 끊기거나 사진 공개 순서·시연 기록이 달라지지 않도록 검증한다.
- 사용자가 중지한 공개 배포는 커밋·푸시로 재개되지 않게 한다.

## 완료 결과

코드 정비 `c56981a`를 개인 원격 main에 반영했습니다. 로컬 빌드·85개 검사·소스 포맷·현재 문서 링크 검증 통과. 320/390/1280px 브라우저에서 게임·지도·사진 역할·정답/오답·진행 복원·시연 분리·QR 공유를 확인했습니다. GitHub의 검사 전용 [Check 실행](https://github.com/ezid007/GDG_Incheon/actions/runs/34744403692)도 성공했습니다.

배포 워크플로는 Disabled이며 이번 커밋은 공개 배포를 실행하지 않았습니다. 공개 사이트404 확인, 재배포는 사용자 재요청을 기다립니다. README·현행 docs·정리 내역·개발 가이드·로컬 로그 갱신 완료. 현재 정리 작업의 남은 항목은 없습니다. 기존 현장 GPS 및 동화마을 전체 근거 사진 확인은 별도 후속 사항입니다.
