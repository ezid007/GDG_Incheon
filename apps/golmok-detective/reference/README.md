# 미사용 참고 자료

현재 문제 데이터에서 참조하지 않는 사진을 보존합니다. 이 폴더는 배포 대상인 `public` 밖에 있으므로 사이트에 함께 게시되지 않습니다.

| 파일 | 보존 이유 |
|---|---|
| `unused-photos/gate/field-gate-hint.jpg` | 현재 문제 데이터와 코드에서 사용하지 않는 이전 힌트 사진 |
| `unused-photos/taletown/field-taletown-hint.jpg` | 현재 `field-taletown-quiz.jpg`와 바이트가 같은 중복 사진 |

2026-09-13에 원본 바이트를 유지한 채 옮겼습니다. 다시 쓸 때는 사진 역할을 검수하고 `public/assets/quiz/`의 해당 문제 폴더에 필요한 파일만 추가합니다. 현장 첨부 전체를 복사하지 않습니다.
